[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --strict-markers
    --html=reports/report.html
    --self-contained-html

markers =
    auth:      Authentication and registration tests
    users:     User CRUD workflow tests
    contract:  JSON Schema contract validation tests
    slow:      Tests that make multiple sequential requests
